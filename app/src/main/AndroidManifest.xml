<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.creamsyapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/creamsy"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CreamsyApp">
        <activity
            android:name=".activity.AuthActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MainActivity"
            android:exported="false" />
        <activity
            android:name=".activity.HistoryActivity"
            android:label="Riwayat Transaksi" />
        <activity
            android:name=".activity.AddProductActivity"
            android:label="Tambah Produk Baru" />
        <activity
            android:name=".activity.ProductManagementActivity"
            android:label="Manajemen Produk" />
    </application>

</manifest>